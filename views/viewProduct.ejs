<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Product Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
  <div class="container mt-5 d-flex justify-content-center">
    <div class="col-md-6">
      <h2 class="mb-4 text-center">Product Details</h2>
      <div class="card p-4 shadow-sm">
        <h4><strong>Name:</strong> <%= product.name %></h4>
        <p><strong>Price:</strong> ₹<%= product.price %></p>
        <p><strong>Product ID:</strong> <code class="text-danger"><%= product._id %></code></p>

         <% if (imageExists) { %>
          <div class="text-center mb-3">
            <img src="/uploads/<%= product.image %>" alt="Product Image" class="img-fluid rounded" style="max-height: 250px;">
          </div>
        <% } else { %>
            <p class="text-center text-muted">No image available</p>
        <% } %>

        <div class="d-flex justify-content-center gap-3 mt-3">
          <a href="/products/<%= product._id %>/edit" class="btn btn-warning btn-sm px-4">Edit</a>
          <a href="/products" class="btn btn-secondary btn-sm px-4">Back</a>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
